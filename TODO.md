# TODO: Implement Show More for Quiz Questions

- [x] Import useState from React in QuizMultipleOptions.jsx
- [x] Add state variable `visibleQuestions` initialized to 10
- [x] Modify the quizquestions.map() to use slice(0, visibleQuestions) for initial display
- [x] Add "Show More" button below the grid, conditionally rendered if more questions exist
- [x] Implement button onClick to set visibleQuestions to full length
